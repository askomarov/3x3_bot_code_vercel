[project]
name = "scorer-bot-vercel"
version = "1.0.0"
requires-python = "==3.12.*"
dependencies = [
    "flask==2.3.3",
    "python-telegram-bot==20.7",
    "python-dotenv==1.0.0",
    "httpx>=0.25.2,<0.28.0",
    "psycopg2-binary==2.9.9",
]

[tool.vercel]
entrypoint = "main:app"
